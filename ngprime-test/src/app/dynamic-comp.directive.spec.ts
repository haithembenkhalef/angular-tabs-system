import { DynamicCompDirective } from './dynamic-comp.directive';

describe('DynamicCompDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCompDirective();
    expect(directive).toBeTruthy();
  });
});
